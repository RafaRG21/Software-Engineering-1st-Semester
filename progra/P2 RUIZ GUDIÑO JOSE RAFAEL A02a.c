#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main() {
	int suma=0;
	while(suma<=175){
		suma=suma+2;
		printf("%i\n",suma);
		suma=suma+3;
		printf("%i\n",suma);
	}
	
	return 0;
}
